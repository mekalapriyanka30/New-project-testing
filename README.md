# Demo Spring Boot Application

Minimal Spring Boot application generated for development.

Build and run (requires Java 17 and Maven):

```bash
mvn clean package
java -jar target/demo-0.0.1-SNAPSHOT.jar
# or during development
mvn spring-boot:run
```

Endpoints:

- `GET /` — returns a greeting string.
